@model IEnumerable<Assignment.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.planes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cranes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.planes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cranes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.trucks }) |
            @Html.ActionLink("Details", "Details", new { id=item.trucks }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.trucks })
        </td>
    </tr>
}

</table>
